#!/bin/sh
# Re-run the protected-path gate against the final commit message, so an
# acknowledgement typed during the commit is seen. The pre-commit hook cannot
# read a message that does not exist yet.
exec node --import tsx scripts/gate.ts --commit-message-file "$1"
